> A continuation is a block that extends the previous block, assuming it's a container of some kind.

	This block is a continuation of the one before it.

	This is in a blockquote, so should be part of that blockquote.

This is a block one-level up.
